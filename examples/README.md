# Cross-pattern examples

This directory is reserved for application-level assemblies that combine patterns, mappings and target policy. Pattern-specific fixtures live with each pattern package. Examples must use fictional, non-sensitive data and state that conformance is not certification.

