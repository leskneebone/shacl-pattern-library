# Catalogue vocabulary

This directory will contain only terms not adequately supplied by established vocabularies. Before defining them, map the contract to DCTERMS, PROF, DCAT, ADMS, SPDX, PROV-O, SHACL and OWL, documenting semantic mismatches.

The first vocabulary release is intentionally deferred until the three experimental pattern manifests reveal which local terms are actually necessary.

