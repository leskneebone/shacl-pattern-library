# ValPub publication profile

This profile will map catalogue records and distributions to ValPub publication requirements without making the publication view the executable module model. Pattern lineage, version, module distribution and generated assembly are distinct resources and may require distinct profiles.

Implementation is deferred pending confirmation of the applicable ValPub specification/version and its mandatory metadata.

